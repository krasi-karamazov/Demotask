<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:animateLayoutChanges="true"
    android:background="@color/mainBackgroundColor">
    <ImageView
        android:id="@+id/iv_logo"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="@dimen/logo_margin_top"
        android:scaleType="centerInside"
        android:src="@drawable/img_you_local_logo"
        android:layout_alignParentTop="true"/>
    <LinearLayout
        android:id="@+id/form_holder"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/iv_logo"
        android:layout_marginTop="@dimen/login_form_margin_top"
        android:orientation="vertical">
        <android.support.design.widget.TextInputLayout
            android:id="@+id/input_layout_username"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textColorHint="@color/text_color_hint">
            <EditText
                android:theme="@style/AppTheme.WhiteEditText"
                android:id="@+id/et_username"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:hint="@string/username_label"
                android:textColor="@color/input_text_color"
                android:singleLine="true"
                />
        </android.support.design.widget.TextInputLayout>

        <android.support.design.widget.TextInputLayout
            android:id="@+id/input_layout_password"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="@dimen/form_inner_margin"
            android:hint="@string/password_label"
            android:textColorHint="@color/text_color_hint">
            <EditText
                android:theme="@style/AppTheme.WhiteEditText"
                android:id="@+id/et_password"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:inputType="textPassword"
                android:textColor="@color/input_text_color"
                android:singleLine="true"
                />
        </android.support.design.widget.TextInputLayout>
    </LinearLayout>

    <android.support.design.widget.TextInputLayout
        android:id="@+id/username_for_password"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/iv_logo"
        android:layout_marginTop="@dimen/login_form_margin_top"
        android:hint="@string/username_label"
        android:visibility="gone"
        android:textColorHint="@color/text_color_hint">
        <EditText
            android:theme="@style/AppTheme.WhiteEditText"
            android:id="@+id/et_username_for_pass"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:inputType="textPassword"
            android:textColor="@color/input_text_color"
            android:singleLine="true"
            />
    </android.support.design.widget.TextInputLayout>

    <Button
        android:id="@+id/btn_login"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="@dimen/login_button_margin_top"
        android:background="@drawable/button_background"
        android:layout_below="@+id/form_holder"
        android:textColor="@color/mainBackgroundColor"
        android:textSize="@dimen/login_label_text_size"
        android:text="@string/login_label"/>

    <TextView
        android:id="@+id/tv_forgotten_password"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center_horizontal"
        android:textColor="@color/colorAccent"
        android:layout_alignParentBottom="true"
        android:layout_marginBottom="@dimen/forgotten_pass_margin_bottom"
        android:textSize="@dimen/forgotten_pass_text_size"
        android:text="@string/forgotten_password_label"/>
</RelativeLayout>